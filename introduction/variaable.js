/*
1. Do not need to use let again
2. Can't use keywords or reserved words
3. No special characters other than $ and _ 4. =is for assignment
*/

let money = 1000;
console.log(money);

/*
  ------  variable (updating values)------

  1. do not need to use let again
  2. variable = variable +1
  3. assinment operator is used =
  4. short hand assignment operator +=,-=,*='+=
 */
let noOfstudents = 5;
noOfstudents = noOfstudents + 1;
console.log(noOfstudents);


let oney =1;
oney += 1
console.log(oney);

let radius = 4;
let pi = 3.14;
let area = pi*radius*radius;
console.log(area);

pi = 34;
let parimeter = 2*pi*radius;
console.log(parimeter);